{
  // Solution file: routes each source file to the right project (Node vs.
  // browser) for editors. Type-check both with `npm run typecheck`.
  "files": [],
  "references": [
    { "path": "./tsconfig.server.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
